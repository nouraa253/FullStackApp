apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fullstack-frontend
  namespace: fullstack
spec:
  ingressClassName: nginx
  rules:
    - host: fullstack.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
